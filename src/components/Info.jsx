import React from 'react'

const Info = ({ children }) => (

    <div className="Info">
        <div className="Infor-container">
            { children }
        </div>
    </div>

);

export default Info;